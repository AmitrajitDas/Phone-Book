import { combineReducers } from 'redux';
import { contactReducer } from './contactReducer';

export default combineReducers({
    contactReducer, // contactReducer : contactReducer (simple es6)
});